baseURL = "https://localhost/local-prod/"
[params]
    data_repo = "http://localhost/EHDP-data/"
    data_branch = "production"

[caches]
    [caches.getresource]
        dir = ':cacheDir/:project'
        maxAge = -1

# [server.headers]
#     for = '/**'
#     [server.headers.values]
#         Access-Control-Allow-Origin = "*"