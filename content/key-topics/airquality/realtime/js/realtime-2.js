// Ingest data feed file from azure using D3
// Deposit into full object and short object for later use.


// Get all values for "station" from object and dump into an array. This gives us all monitor locations.
// For each station name, get lats and longs from monitor_locations.csv

// create leaflet map using lats and longs.


/* for each station name with enough data...:
    Create a json mark object to insert into VL Spec (use color from monitor_locations.csv)
    Create a button and inset into HTML (use color...)
    ? What counts as "enough data"? 
*/



// Create leaflet filter/zooms

// Create "show/hide DEC monitors" button: if operator = DEC, hide from 